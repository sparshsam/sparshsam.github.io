# sparshsam.github.io — Claude Code Instructions

## Overview

Personal GitHub Pages landing site. Pure HTML + CSS — no framework, no build step.

## Rules

1. Static site. No build system, no dependencies.
2. Update README.md when ecosystem projects change.
3. Keep index.html and styles.css in sync.
4. Branch naming: \`feat/*\`, \`fix/*\`, \`docs/*\`.
